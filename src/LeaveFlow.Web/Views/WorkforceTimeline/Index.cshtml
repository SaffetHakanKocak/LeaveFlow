@model WorkforceTimelineViewModel
@{
    ViewData["Title"] = "Workforce Timeline";
}

<div class="d-flex flex-wrap align-items-center justify-content-between gap-2 mb-3">
    <div>
        <h1 class="h3 mb-1">Workforce Timeline</h1>
        <p class="text-secondary mb-0">Approved leave visibility for the selected workforce scope.</p>
    </div>
</div>

<form method="get" class="lf-panel mb-3">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="row g-2 align-items-end">
        <div class="col-md-2">
            <label class="form-label" for="startDate">Start</label>
            <input class="form-control" id="startDate" name="startDate" type="date" value="@Model.StartDate?.ToString("yyyy-MM-dd")" />
            <span class="text-danger field-validation-valid" data-valmsg-for="StartDate"></span>
        </div>
        <div class="col-md-2">
            <label class="form-label" for="endDate">End</label>
            <input class="form-control" id="endDate" name="endDate" type="date" value="@Model.EndDate?.ToString("yyyy-MM-dd")" />
            <span class="text-danger field-validation-valid" data-valmsg-for="EndDate"></span>
        </div>
        <div class="col-md-3">
            <label class="form-label" for="consultantSearch">Consultant</label>
            <input class="form-control" id="consultantSearch" name="consultantSearch" value="@Model.ConsultantSearch" />
        </div>
        <div class="col-md-3">
            <label class="form-label" for="managerId">Manager filter</label>
            <input class="form-control" id="managerId" name="managerId" value="@Model.ManagerId" />
        </div>
        <div class="col-md-2">
            <div class="form-check mb-2">
                <input class="form-check-input" type="checkbox" id="includeInactive" name="includeInactive" value="true" checked="@Model.IncludeInactive" />
                <label class="form-check-label" for="includeInactive">Include inactive</label>
            </div>
            <button class="btn btn-outline-primary w-100" type="submit">Apply</button>
        </div>
    </div>
</form>

@if (Model.Timeline is null)
{
    <div class="lf-panel">
        <div class="lf-empty-state text-secondary">Choose a date range up to @LeaveFlow.Application.Timeline.WorkforceTimelineSettings.MaxRangeDays days.</div>
    </div>
}
else if (Model.Timeline.Rows.Count == 0)
{
    <div class="lf-panel">
        <div class="lf-empty-state text-secondary">No consultants found for this scope and filter.</div>
    </div>
}
else
{
    <div class="lf-panel mb-3">
        <div class="d-flex flex-wrap gap-3 small">
            <span><span class="lf-legend lf-legend-leave"></span> Approved leave</span>
            <span><span class="lf-legend lf-legend-weekend"></span> Weekend</span>
            <span><span class="lf-legend lf-legend-today"></span> Today</span>
        </div>
    </div>

    <div class="table-responsive lf-panel lf-timeline-wrap">
        <table class="table table-sm align-middle mb-0 lf-timeline">
            <thead>
                <tr>
                    <th class="lf-sticky-col">Consultant</th>
                    @foreach (var day in Model.Timeline.Days)
                    {
                        <th class="text-center @(day.IsWeekend ? "lf-weekend" : string.Empty) @(day.IsToday ? "lf-today" : string.Empty)">
                            <div>@day.Date.ToString("MM-dd")</div>
                            <div class="text-secondary small">@day.Date.ToString("ddd")</div>
                        </th>
                    }
                </tr>
            </thead>
            <tbody>
            @foreach (var row in Model.Timeline.Rows)
            {
                <tr>
                    <th class="lf-sticky-col">
                        <div>@row.ConsultantName</div>
                        <div class="text-secondary small">@row.ConsultantEmail</div>
                        @if (!row.ConsultantIsActive)
                        {
                            <span class="badge text-bg-secondary">Inactive</span>
                        }
                    </th>
                    @foreach (var cell in row.Cells)
                    {
                        var title = cell.IsOnLeave
                            ? $"{row.ConsultantName} on leave: {cell.Reason}"
                            : $"{row.ConsultantName}: available";
                        <td class="text-center @(cell.IsWeekend ? "lf-weekend" : string.Empty) @(cell.IsToday ? "lf-today" : string.Empty) @(cell.IsOnLeave ? "lf-leave" : string.Empty)" title="@title">
                            <span class="visually-hidden">@title</span>
                            @(cell.IsOnLeave ? "Leave" : string.Empty)
                        </td>
                    }
                </tr>
            }
            </tbody>
        </table>
    </div>

    @if (Model.Timeline.TotalPages > 1)
    {
        <nav class="mt-3" aria-label="Pagination">
            <span class="text-secondary">Page @Model.Timeline.PageNumber of @Model.Timeline.TotalPages</span>
        </nav>
    }
}
