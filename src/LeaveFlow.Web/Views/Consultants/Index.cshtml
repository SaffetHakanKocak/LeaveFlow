@model ConsultantListViewModel
@{
    ViewData["Title"] = "Danışmanlar";
}

<div class="d-flex flex-wrap align-items-center justify-content-between gap-2 mb-3">
    <div>
        <h1 class="h3 mb-1">Danışmanlar</h1>
        <p class="text-secondary mb-0">Danışman profillerini ve hesap durumunu yönetin.</p>
    </div>
    <a class="btn btn-primary" asp-action="Create">Oluştur</a>
</div>

<form method="get" class="lf-panel mb-3">
    <div class="row g-2 align-items-end">
        <div class="col-md-6">
            <label class="form-label" for="search">Ara</label>
            <input class="form-control" id="search" name="search" value="@Model.Search" />
        </div>
        <div class="col-md-3">
            <label class="form-label" for="isActive">Durum</label>
            <select class="form-select" id="isActive" name="isActive">
                <option value="">Tümü</option>
                <option value="true" selected="@(Model.IsActive == true)">Aktif</option>
                <option value="false" selected="@(Model.IsActive == false)">Pasif</option>
            </select>
        </div>
        <div class="col-md-3">
            <button class="btn btn-outline-primary w-100" type="submit">Filtrele</button>
        </div>
    </div>
</form>

<div class="table-responsive lf-panel">
    <table class="table align-middle mb-0">
        <thead>
            <tr>
                <th>Ad Soyad</th>
                <th>E-posta</th>
                <th>Personel No</th>
                <th>Departman</th>
                <th>Durum</th>
                <th class="text-end">İşlemler</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var consultant in Model.Results.Items)
        {
            <tr>
                <td>@consultant.FirstName @consultant.LastName</td>
                <td>@consultant.Email</td>
                <td>@consultant.EmployeeNumber</td>
                <td>@consultant.Department</td>
                <td><span class="badge @(consultant.IsActive ? "text-bg-success" : "text-bg-secondary")">@UiText.Active(consultant.IsActive)</span></td>
                <td class="text-end">
                    <div class="lf-action-row">
                        <a class="btn btn-sm btn-outline-secondary" asp-action="Details" asp-route-id="@consultant.Id">Detay</a>
                        <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@consultant.Id">Düzenle</a>
                        <form asp-action="SetActive" asp-route-id="@consultant.Id" method="post" class="d-inline">
                            <input type="hidden" name="isActive" value="@(!consultant.IsActive)" />
                            <button class="btn btn-sm btn-outline-warning" type="submit">@(consultant.IsActive ? "Pasifleştir" : "Aktifleştir")</button>
                        </form>
                    </div>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>

<partial name="_Pagination" model="Model.Results" />
